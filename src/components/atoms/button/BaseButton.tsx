import styled from 'styled-components';

export const BaseButton = styled.button`
	color: #fff;
	width: 80%;
	padding: 6px 24px;
	border: none;
	border-radius: 8px;
	outline: none;
	&:hover {
		cursor: pointer;
		opacity: 0.8;
	}
`;